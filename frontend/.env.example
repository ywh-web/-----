# Local development defaults to http://127.0.0.1:8000/api when omitted.
# Set this to the public Render backend URL for a production build.
VITE_API_BASE_URL=https://your-render-service.onrender.com/api
